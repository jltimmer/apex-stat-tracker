import Route from '@ember/routing/route';

export default class PlayerStatsRoute extends Route {
  model(params) {
    // let apiKey = "";
    // const response = await fetch(`https://api.mozambiquehe.re/bridge?version=5&platform=PC&player=${username}&auth=${apiKey}`);
    // const playerStats = await response.json();
    // console.log(ENV[TEST_KEY]);
    // fetch(`https://api.mozambiquehe.re/bridge?version=5&platform=PC&player=${username}&auth=${apiKey}`)
    //   .then(response => response.json())
    //   .then(data => console.log(data));
    // return playerStats;
  }
}
